# Kaggle / Colab Training Dependencies
# These are installed on Kaggle T4 — do NOT install locally
# Use requirements-dev.txt for local CPU development

unsloth[kaggle-new]
trl>=0.9.0
transformers>=4.45.0
datasets>=2.20.0
peft>=0.12.0
accelerate>=0.33.0
# torch>=2.3.0 is pre-installed by Kaggle

# Evaluation
evaluate>=0.4.0
rouge-score
sacrebleu

# Utilities
wandb  # optional — for experiment tracking
huggingface_hub>=0.23.0

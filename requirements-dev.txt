# Local Development Dependencies (CPU-only, no GPU required)
# For VS Code local dev and smoke testing ONLY
# DO NOT use on Kaggle — use requirements.txt there

# Core ML (CPU only — no unsloth which requires CUDA)
transformers>=4.45.0
datasets>=2.20.0
tokenizers
peft>=0.12.0
accelerate>=0.33.0

# Data generation via APIs
google-genai>=1.0.0         # Primary — Gemini 2.0 Flash
anthropic>=0.29.0           # Fallback
openai>=1.30.0              # Fallback

# Environment / secrets
python-dotenv>=1.0.0

# Demo / inference
gradio>=4.0.0
smolagents>=1.0.0

# Evaluation
evaluate>=0.4.0
rouge-score
sacrebleu

# Testing & linting
pytest>=8.0.0
pytest-timeout
ruff>=0.4.0
mypy>=1.10.0

# Utilities
huggingface_hub>=0.23.0
pydantic>=2.0.0
pyyaml>=6.0
tqdm
